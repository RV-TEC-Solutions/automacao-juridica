"use client";

import { CaretDown, FunnelSimple, MagnifyingGlass } from "@phosphor-icons/react";
import { FormEvent, useCallback, useEffect, useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { AppShell } from "../components/app-shell";
import { ExpedienteDrawer } from "../components/expediente-drawer";
import { ExpedienteList } from "../components/expediente-list";
import { Feedback, LoadingRows, PageTitle } from "../components/ui";
import { api } from "../lib/api";
import type { Expediente } from "../lib/types";

type PageData = { count: number; next: string | null; previous: string | null; results: Expediente[] };
const selectClass = "control w-full px-3 text-xs font-semibold";

export function ExpedientesClient() {
  const search = useSearchParams(); const router = useRouter(); const [data, setData] = useState<PageData | null>(null); const [selected, setSelected] = useState<Expediente | null>(null); const [error, setError] = useState("");
  const query = search.toString();
  const load = useCallback(async () => { try { setData(await api<PageData>(`expedientes/?${query}`)); setError(""); } catch (exception) { setError(exception instanceof Error ? exception.message : "Falha ao carregar."); } }, [query]);
  useEffect(() => { queueMicrotask(() => { void load(); }); }, [load]);
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const params = new URLSearchParams(); for (const [key, value] of form.entries()) if (value) params.set(key, String(value)); router.replace(`/expedientes?${params}`); };
  const page = Number(search.get("page") ?? 1); const pages = Math.max(1, Math.ceil((data?.count ?? 0) / 25));
  const go = (nextPage: number) => { const params = new URLSearchParams(search); params.set("page", String(nextPage)); router.replace(`/expedientes?${params}`); };
  return <AppShell><PageTitle eyebrow="Consulta operacional" title="Expedientes" description="Pesquise, filtre e acompanhe alterações capturadas nas fontes conectadas." />
    <form className="bezel-card mb-5" onSubmit={submit}><div className="bezel-inner overflow-hidden"><div className="flex flex-col gap-3 border-b border-rule bg-panel-muted/70 p-4 sm:flex-row"><label className="relative flex-1"><MagnifyingGlass size={18} className="absolute top-1/2 left-3 -translate-y-1/2 text-quiet" /><input className="control w-full py-2 pr-3 pl-10 text-sm" name="q" defaultValue={search.get("q") ?? ""} placeholder="Processo, partes ou assunto" aria-label="Buscar expedientes" /></label><button className="button-primary cursor-pointer px-4 text-sm"><MagnifyingGlass size={16} weight="bold" />Buscar</button></div><details className="group"><summary className="flex cursor-pointer list-none items-center justify-between px-4 py-3 text-xs font-extrabold text-ink"><span className="flex items-center gap-2"><FunnelSimple size={17} className="text-brand" />Filtros avançados {query && <span className="rounded-full bg-brand-soft px-2 py-0.5 text-[10px] text-brand">ativos</span>}</span><CaretDown size={16} className="text-quiet transition-transform group-open:rotate-180" /></summary><div className="grid grid-cols-1 gap-3 border-t border-rule p-4 sm:grid-cols-2 lg:grid-cols-4"><select className={selectClass} name="source" defaultValue={search.get("source") ?? ""} aria-label="Fonte"><option value="">Todas as fontes</option><option value="pje-tjrn">PJe 1º Grau · TJRN</option><option value="pje2g-tjrn">PJe 2º Grau · TJRN</option></select><select className={selectClass} name="pending_type" defaultValue={search.get("pending_type") ?? ""} aria-label="Pendência"><option value="">Todas as pendências</option><option value="ciencia">Ciência</option><option value="resposta">Resposta</option><option value="nao_identificada">Não identificada</option></select><select className={selectClass} name="deadline" defaultValue={search.get("deadline") ?? ""} aria-label="Prazo"><option value="">Todos os prazos</option><option value="urgent">Urgentes</option><option value="overdue">Vencidos</option><option value="future">Futuros</option><option value="calculating">Em cálculo</option><option value="none">Sem prazo</option><option value="resolved">Resolvidos</option></select><select className={selectClass} name="read" defaultValue={search.get("read") ?? ""} aria-label="Leitura"><option value="">Lidos e não lidos</option><option value="unread">Não lidos</option><option value="read">Lidos</option></select><input className={selectClass} name="date_from" type="date" defaultValue={search.get("date_from") ?? ""} aria-label="Eventos a partir de" /><input className={selectClass} name="date_to" type="date" defaultValue={search.get("date_to") ?? ""} aria-label="Eventos até" /><select className={selectClass} name="ordering" defaultValue={search.get("ordering") ?? "recent"} aria-label="Ordenação"><option value="recent">Mais recentes</option><option value="deadline">Prazo mais próximo</option><option value="expedition">Expedição recente</option><option value="process">Número do processo</option></select><button className="button-secondary cursor-pointer px-4 text-xs" onClick={() => router.replace("/expedientes")} type="button">Limpar filtros</button></div></details></div></form>
    {error && <Feedback action={<button className="button-secondary cursor-pointer px-3 text-xs" onClick={load}>Tentar novamente</button>}>{error}</Feedback>}
    <div className="mb-3 flex items-center justify-between text-xs text-quiet"><span>{data ? `${data.count} expediente${data.count === 1 ? "" : "s"} encontrado${data.count === 1 ? "" : "s"}` : "Carregando consulta…"}</span>{query && <button className="cursor-pointer border-0 bg-transparent font-extrabold text-brand" onClick={() => router.replace("/expedientes")}>Limpar todos</button>}</div>
    {data ? <ExpedienteList items={data.results} onSelect={setSelected} /> : <LoadingRows />}
    {data && pages > 1 && <nav className="mt-5 flex items-center justify-center gap-4 text-xs text-quiet" aria-label="Paginação"><button className="button-secondary cursor-pointer px-3 text-xs disabled:opacity-40" disabled={page <= 1} onClick={() => go(page - 1)}>← Anterior</button><span>Página <strong className="font-[family-name:var(--font-mono)] text-ink">{page}</strong> de {pages}</span><button className="button-secondary cursor-pointer px-3 text-xs disabled:opacity-40" disabled={page >= pages} onClick={() => go(page + 1)}>Próxima →</button></nav>}
    <ExpedienteDrawer item={selected} onClose={() => setSelected(null)} onRead={() => { if (selected) setSelected({ ...selected, unread: false }); load(); }} />
  </AppShell>;
}
